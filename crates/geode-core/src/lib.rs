//! `geode-core` — GDE1 format, crypto, vault, policy evaluate.
//!
//! The Geode API surface (SURFACE-TRIPLE: the library *is* the API; CLI and
//! later MCP/TUI are adapters over this crate).
//!
//! G0 scaffold stub. Backend (G0b/G1/G2) fills the module skeleton per spec
//! `13-implementation-sketch.md` (`kdf`, `aead`, `wrap`, `chunk`, `name`,
//! `manifest`, `recipients`, `policy`, `token`, `zero`). No crypto ships in
//! G0; anything present here must fail closed.
